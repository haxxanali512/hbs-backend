<div class="min-h-screen bg-gray-50 py-8">
  <div class="w-full mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Header -->
    <div class="mb-8">
      <div class="flex justify-between items-center">
        <div>
          <h1 class="text-3xl font-bold text-gray-900">Provider Notes</h1>
          <p class="mt-2 text-gray-600">View provider notes for encounter</p>
        </div>
        <div class="flex space-x-3">
          <%= link_to "Back to Encounter", tenant_encounter_path(@encounter), class: "bg-gray-600 hover:bg-gray-700 text-white font-medium py-2 px-4 rounded-lg transition-colors" %>
        </div>
      </div>
    </div>

    <!-- Provider Notes -->
    <%= render "shared/provider_notes", namespace: "tenant" %>
  </div>
</div>

