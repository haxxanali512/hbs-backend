<%# Shared Specialty form for Tenant %>
<div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
  <% if @specialty.errors.any? %>
    <div class="mb-6 bg-red-50 border border-red-200 text-red-800 rounded-md p-4">
      <div class="font-semibold mb-2"><%= pluralize(@specialty.errors.count, "error") %> prohibited this specialty from being saved:</div>
      <ul class="list-disc ml-6 text-sm">
        <% @specialty.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form_with(model: @specialty, url: (@specialty.new_record? ? tenant_specialties_path : tenant_specialty_path(@specialty)), local: true, class: "space-y-6") do |f| %>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <%= f.label :name, class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.text_field :name, class: "w-full border border-gray-300 rounded-md px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500" %>
      </div>
      <div>
        <%= f.label :status, class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.select :status, options_for_select([["Active", "active"], ["Retired", "retired"]], @specialty.status || "active"), {}, class: "w-full border border-gray-300 rounded-md px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500" %>
      </div>
    </div>

    <div>
      <%= f.label :description, class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.text_area :description, rows: 4, class: "w-full border border-gray-300 rounded-md px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500" %>
    </div>

    <!-- CPT Codes Selection -->
    <div>
      <%= f.label :procedure_code_ids, "Allowed CPT Codes", class: "block text-sm font-medium text-gray-700 mb-2" %>
      <p class="text-sm text-gray-500 mb-2">Select any CPT codes from the global catalog that should be allowed for this specialty.</p>
      <div class="border border-gray-300 rounded-md p-4 max-h-64 overflow-y-auto">
        <% if @procedure_codes.any? %>
          <div class="space-y-2">
            <% @procedure_codes.each do |code| %>
              <label class="flex items-start space-x-3 p-2 hover:bg-gray-50 rounded">
                <%= check_box_tag "specialty[procedure_code_ids][]", code.id, 
                    @specialty.procedure_codes.include?(code),
                    class: "mt-1 h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded" %>
                <div class="flex-1 min-w-0">
                  <div class="text-sm font-medium text-gray-900 font-mono"><%= code.code %></div>
                  <div class="text-sm text-gray-500"><%= code.description %></div>
                </div>
              </label>
            <% end %>
          </div>
        <% else %>
          <div class="text-center py-8 text-gray-500">
            <p>No procedure codes available</p>
            <p class="text-sm mt-1">Procedure codes must be unlocked for your organization first</p>
          </div>
        <% end %>
      </div>
      <p class="mt-2 text-sm text-gray-500">Select the CPT codes that are allowed for this specialty</p>
    </div>

    <div class="flex items-center space-x-3">
      <%= f.submit (@specialty.new_record? ? "Create Specialty" : "Update Specialty"), 
          class: "bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 px-4 rounded-md transition-colors" %>
      <%= link_to "Cancel", (@specialty.new_record? ? tenant_specialties_path : tenant_specialty_path(@specialty)), 
          class: "text-gray-600 hover:text-gray-800" %>
    </div>
  <% end %>
</div>

