<div class="min-h-screen bg-gray-50 py-8">
  <div class="mx-auto w-full max-w-3xl px-4 sm:px-6 lg:px-8">
    <h1 class="text-2xl font-bold text-gray-900">Onboarding Payment</h1>
    <p class="mt-1 text-sm text-gray-600">Step 3 of 4: add a Stripe card and pay the one-time $500 onboarding fee.</p>

    <div class="mt-6 rounded-lg border border-gray-200 bg-white p-6">
      <div class="space-y-3 text-sm text-gray-700">
        <p><span class="font-medium">Payment method:</span> Stripe card or GoCardless mandate</p>
        <p><span class="font-medium">Onboarding fee:</span> $500 (one-time)</p>
        <p><span class="font-medium">Stripe status:</span>
          <% if @billing&.stripe_payment_method_id.present? %>
            Saved ending in <%= @billing.method_last4 %>
          <% else %>
            No card saved yet
          <% end %>
        </p>
        <p><span class="font-medium">GoCardless status:</span>
          <% if @billing&.gocardless_customer_id.present? && @billing&.gocardless_mandate_id.present? %>
            Authorized mandate <%= @billing.gocardless_mandate_id %>
          <% else %>
            No mandate authorized yet
          <% end %>
        </p>
      </div>

      <div class="mt-5 flex flex-wrap items-center gap-3">
        <%= link_to "Add/Update Card", tenant_activation_stripe_card_path, class: "inline-flex items-center rounded-md border border-gray-300 bg-white px-4 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50" %>

        <%= form_with url: "/tenant/gocardless/create_redirect_flow", method: :post, local: true, class: "inline" do |g| %>
          <%= g.hidden_field :given_name, value: current_user.first_name %>
          <%= g.hidden_field :family_name, value: current_user.last_name %>
          <%= g.hidden_field :email, value: current_user.email %>
          <%= g.hidden_field :address_line1, value: @current_organization.organization_contact&.address_line1.to_s.presence || "Address required" %>
          <%= g.hidden_field :city, value: @current_organization.organization_contact&.city.to_s.presence || "City" %>
          <%= g.hidden_field :postal_code, value: @current_organization.organization_contact&.zip.to_s.presence || "00000" %>
          <%= g.hidden_field :country_code, value: (@current_organization.organization_contact&.country.presence || "US") %>
          <%= g.hidden_field :context, value: "activation" %>
          <%= g.submit "Authorize GoCardless", class: "inline-flex items-center rounded-md border border-gray-300 bg-white px-4 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50" %>
        <% end %>
      </div>

      <div class="mt-4">
        <%= form_with model: [@current_organization, @billing], url: tenant_activation_billing_path, method: :patch, local: true do |form| %>
          <div class="mb-3">
            <%= form.label :provider, "Charge using", class: "mb-1 block text-sm font-medium text-gray-700" %>
            <%= form.select :provider, options_for_select([["Stripe", "stripe"], ["GoCardless", "gocardless"]], @billing.provider || "stripe"), {}, class: "block w-full rounded-md border-gray-300 text-sm md:w-64" %>
          </div>
          <%= form.submit "Charge $500 & Continue", class: "inline-flex items-center rounded-md bg-blue-600 px-4 py-2 text-sm font-medium text-white hover:bg-blue-700" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
