<div class="min-h-screen bg-gray-50">
  <!-- Header -->
  <div class="bg-white border-b border-gray-200">
    <div class="px-6 py-4">
      <div class="flex items-center justify-between">
        <div class="flex items-center space-x-3">
          <div class="w-10 h-10 bg-blue-600 rounded-lg flex items-center justify-center">
            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
          <div>
            <h1 class="text-xl font-semibold text-gray-900">Organization Activation</h1>
            <p class="text-sm text-gray-500">Complete the setup process to activate your organization</p>
          </div>
        </div>
        <div class="flex items-center space-x-3">
          <div class="text-right">
            <p class="text-sm font-medium text-gray-900"><%= current_user.email %></p>
            <p class="text-xs text-gray-500">Account Manager</p>
          </div>
          <div class="w-8 h-8 bg-blue-600 rounded-full flex items-center justify-center">
            <span class="text-white font-medium text-sm"><%= current_user.email.first.upcase %></span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Main Content -->
  <div class="mw-full mx-auto py-8 px-4 sm:px-6 lg:px-8">
    <div class="space-y-6">
      <!-- Progress Card -->
      <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
        <h3 class="text-lg font-semibold text-gray-900 mb-4">Activation Progress</h3>
        <div class="flex items-center justify-between mb-2">
          <span class="text-sm font-medium text-gray-700">Overall Progress</span>
          <span class="text-sm font-medium text-gray-700"><%= @organization.activation_progress_percentage %>%</span>
        </div>
        <div class="w-full bg-gray-200 rounded-full h-2">
          <div class="bg-blue-600 h-2 rounded-full transition-all duration-300" style="width: <%= @organization.activation_progress_percentage %>%"></div>
        </div>
      </div>

      <!-- Steps -->
      <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
        <h3 class="text-lg font-semibold text-gray-900 mb-6">Activation Steps</h3>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-4">
          <% @steps.each_with_index do |step, index| %>
            <div class="relative">
              <!-- Step Card -->
              <div class="<%= step[:completed] ? 'bg-green-50 border-green-200' : step[:current] ? 'bg-blue-50 border-blue-200' : 'bg-gray-50 border-gray-200' %> rounded-lg border p-4 text-center">
                <!-- Step Number/Icon -->
                <div class="mx-auto flex h-8 w-8 items-center justify-center rounded-full <%= step[:completed] ? 'bg-green-600' : step[:current] ? 'bg-blue-600' : 'bg-gray-400' %> mb-3">
                  <% if step[:completed] %>
                    <svg class="h-5 w-5 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="3">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
                    </svg>
                  <% else %>
                    <span class="text-sm font-semibold text-white"><%= index + 1 %></span>
                  <% end %>
                </div>
                
                <!-- Step Name -->
                <h4 class="text-xs font-semibold <%= step[:completed] ? 'text-green-900' : step[:current] ? 'text-blue-900' : 'text-gray-900' %>">
                  <%= step[:name] %>
                </h4>
                
                <!-- Status -->
                <p class="mt-1 text-xs font-medium <%= step[:completed] ? 'text-green-700' : step[:current] ? 'text-blue-700' : 'text-gray-600' %>">
                  <% if step[:completed] %>
                    Complete
                  <% elsif step[:current] %>
                    Current Step
                  <% else %>
                    Pending
                  <% end %>
                </p>
              </div>

              <!-- Connector Line -->
              <% unless index == @steps.length - 1 %>
                <div class="hidden lg:block absolute top-1/2 left-full w-full h-0.5 bg-gray-200 transform -translate-y-1/2 z-0" style="width: calc(100% - 1rem); margin-left: 0.5rem;"></div>
              <% end %>
            </div>
          <% end %>
        </div>
      </div>

      <!-- Current Step Actions -->
      <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
        <% case @organization.activation_status %>
        <% when 'pending' %>
          <h3 class="text-lg font-semibold text-gray-900 mb-2">Ready to get started?</h3>
          <p class="text-gray-600 mb-6">Let's begin by setting up your compliance information (GSA/BAA agreements).</p>
          <%= link_to "Start Compliance Setup", tenant_activation_compliance_path, class: "inline-flex items-center px-4 py-2 text-sm font-medium text-white bg-blue-600 border border-transparent rounded-md shadow-sm hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
        <% when 'compliance_setup' %>
          <h3 class="text-lg font-semibold text-gray-900 mb-2">Next: Billing Setup</h3>
          <p class="text-gray-600 mb-6">Set up your payment method to continue.</p>
          <%= link_to "Continue to Billing", tenant_activation_billing_path, class: "inline-flex items-center px-4 py-2 text-sm font-medium text-white bg-blue-600 border border-transparent rounded-md shadow-sm hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
        <% when 'billing_setup' %>
          <h3 class="text-lg font-semibold text-gray-900 mb-2">Next: Terms & Conditions</h3>
          <p class="text-gray-600 mb-6">Accept the Terms of Use and Privacy Policy to continue.</p>
          <%= link_to "Continue to Terms & Conditions", tenant_activation_documents_path, class: "inline-flex items-center px-4 py-2 text-sm font-medium text-white bg-blue-600 border border-transparent rounded-md shadow-sm hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
        <% when 'terms_agreement' %>
          <h3 class="text-lg font-semibold text-gray-900 mb-2">🎉 Ready to Activate!</h3>
          <p class="text-gray-600 mb-6">All steps completed. Activate your organization to get started.</p>
          <%= link_to "Activate Organization", tenant_activation_complete_path, class: "inline-flex items-center px-4 py-2 text-sm font-medium text-white bg-green-600 border border-transparent rounded-md shadow-sm hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
