<!DOCTYPE html>
<html>
  <head>
    <title>Holistic Business Solution Inc.</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= turbo_include_tags %>
    <!-- Inter font -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <%= favicon_link_tag asset_path("HBS-logo-scaled (1).png") %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <script src="https://js.stripe.com/v3" data-turbo-track="reload"></script>
    <%= javascript_include_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="min-h-screen bg-gray-50">
    <% if user_signed_in? %>
      <div class="min-h-screen">
        <!-- Sidebar -->
        <%= render 'shared/sidebar' %>

        <!-- Main area with topbar -->
        <div class="md:ml-64 min-h-screen flex flex-col">
          <!-- Topbar -->
          <header class="sticky top-0 z-30 bg-white border-b border-gray-200">
            <div class="h-16 flex items-center justify-between px-4 md:px-8">
              <div class="flex items-center gap-3 flex-1">
                <button type="button" class="md:hidden inline-flex h-9 w-9 items-center justify-center rounded-md border border-gray-200 text-gray-600 hover:bg-gray-50">
                  <span class="block h-0.5 w-4 bg-current"></span>
                </button>
                <div class="hidden sm:flex flex-1 max-w-xl">
                  <div class="relative w-full">
                    <input type="text" placeholder="Search..." class="w-full rounded-md border border-gray-200 pl-10 pr-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500" />
                    <span class="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400">
                      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="h-5 w-5"><path fill-rule="evenodd" d="M10.5 3.75a6.75 6.75 0 104.243 12.013l4.247 4.247a.75.75 0 101.06-1.06l-4.247-4.247A6.75 6.75 0 0010.5 3.75zm-5.25 6.75a5.25 5.25 0 1110.5 0 5.25 5.25 0 01-10.5 0z" clip-rule="evenodd"/></svg>
                    </span>
                  </div>
                </div>
              </div>
              <div class="flex items-center gap-3">
                <%= render 'shared/notifications' %>
                <div class="flex items-center gap-3 pl-3 ml-3 border-l border-gray-200">
                  <div class="text-right hidden sm:block">
                    <div class="text-sm font-medium text-gray-900"><%= current_user.display_name %></div>
                    <div class="text-xs text-gray-500"><%= current_user.email %></div>
                  </div>
                  <div class="h-9 w-9 rounded-full bg-indigo-600 text-white flex items-center justify-center text-sm font-semibold">
                    <%= current_user.display_name.to_s.first %>
                  </div>
                </div>
              </div>
            </div>
          </header>

          <!-- Masquerade Banner -->
          <% if respond_to?(:user_masquerade?) && user_masquerade? %>
            <div class="bg-purple-600 text-white px-4 md:px-8 py-3 flex items-center justify-between">
              <div class="flex items-center gap-2">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="h-5 w-5">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                </svg>
                <span class="text-sm font-medium">
                  You are impersonating <strong><%= current_user.display_name %></strong>
                </span>
              </div>
              <%= link_to back_masquerade_path(current_user), 
                  class: "inline-flex items-center gap-2 px-3 py-1.5 bg-white text-purple-600 rounded-md text-sm font-medium hover:bg-purple-50 transition-colors" do %>
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="h-4 w-4">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
                </svg>
                Stop Impersonating
              <% end %>
            </div>
          <% end %>

          <!-- Content -->
          <main class="flex-1 p-4 md:p-8 bg-gray-50">
            <div class="w-full mx-auto">
              <div id="flash_container">
                <%= render 'shared/toast_flash' %>
              </div>
              <%= yield %>
            </div>
          </main>
        </div>
      </div>
    <% else %>
      <main>
        <%= yield %>
      </main>
    <% end %>
    <%= render 'shared/confirm_modal' %>
  </body>
</html>