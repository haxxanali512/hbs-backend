<%= form_with model: [:admin, @organization], class: "space-y-6" do |f| %>
  <div class="rounded-2xl border border-gray-200 bg-white p-8 shadow-sm">
    <h3 class="text-base font-semibold text-gray-900 mb-4">Organization Details</h3>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <label class="block text-sm font-medium text-gray-700">Organization Name</label>
        <%= f.text_field :name, class: "mt-2 block w-full h-11 rounded-lg border border-gray-200 bg-white px-4 text-sm placeholder-gray-400 shadow-sm focus:border-indigo-500 focus:ring-indigo-500" %>
      </div>
      <div>
        <label class="block text-sm font-medium text-gray-700">Subdomain</label>
        <div class="mt-2 flex rounded-lg shadow-sm">
          <%= f.text_field :subdomain, class: "block w-full h-11 rounded-l-lg border border-gray-200 bg-white px-4 text-sm placeholder-gray-400 focus:border-indigo-500 focus:ring-indigo-500" %>
          <span class="inline-flex items-center px-3 rounded-r-lg border border-l-0 border-gray-200 bg-gray-50 text-sm text-gray-500">
            .localhost:3000
          </span>
        </div>
      </div>
      <div>
        <label class="block text-sm font-medium text-gray-700">Tier</label>
        <%= f.select :tier,
            options_for_select([
              ['Basic', 'basic'],
              ['Standard', 'standard'],
              ['Premium', 'premium']
            ], @organization.tier),
            { prompt: 'Select tier' },
            { class: "mt-2 block w-full h-11 rounded-lg border border-gray-200 bg-white px-4 text-sm placeholder-gray-400 shadow-sm focus:border-indigo-500 focus:ring-indigo-500" } %>
      </div>
      <div>
        <label class="block text-sm font-medium text-gray-700">Owner</label>
        <%= f.collection_select :owner_id, 
            @users, 
            :id, 
            ->(user) { "#{user.display_name} (#{user.email})" }, 
            { prompt: "Select owner" }, 
            { class: "mt-2 block w-full h-11 rounded-lg border border-gray-200 bg-white px-4 text-sm placeholder-gray-400 shadow-sm focus:border-indigo-500 focus:ring-indigo-500" } %>
      </div>
    </div>
    <div class="mt-6 flex items-center justify-end gap-4">
      <%= link_to "Cancel", admin_organizations_path, class: "text-sm text-gray-700 hover:text-gray-900" %>
      <%= f.submit (@organization.persisted? ? "Update Organization" : "Create Organization"), class: "inline-flex items-center rounded-lg bg-indigo-600 px-5 py-2.5 text-sm font-medium text-white shadow-sm hover:bg-indigo-700" %>
    </div>
  </div>

  <!-- Information Box -->
  <div class="rounded-2xl border border-blue-200 bg-blue-50 p-6 shadow-sm">
    <div class="flex">
      <div class="flex-shrink-0">
        <svg class="h-5 w-5 text-blue-400" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd" />
        </svg>
      </div>
      <div class="ml-3">
        <h3 class="text-sm font-medium text-blue-800">Important Note</h3>
        <div class="mt-2 text-sm text-blue-700">
          <p>After creating the organization, the owner will need to complete the activation process by visiting their organization's subdomain. This includes:</p>
          <ul class="mt-2 list-disc pl-5 space-y-1">
            <li>Setting up billing information</li>
            <li>Completing compliance requirements</li>
            <li>Signing necessary documents</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
<% end %>
