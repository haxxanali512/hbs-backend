<div class="min-h-screen bg-gray-50 py-8">
  <div class="w-full mx-auto px-4 sm:px-6 lg:px-8">
    <div class="mb-8 flex items-center justify-between">
      <div>
        <h1 class="text-3xl font-bold text-gray-900">Email Template Keys</h1>
        <p class="mt-2 text-gray-600">Manage reusable keys and default content for transactional emails.</p>
      </div>
      <%= link_to "New Template Key", new_admin_email_template_key_path, class: "bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 px-4 rounded-lg transition-colors" %>
    </div>

    <div class="mb-6 rounded-lg border border-blue-200 bg-blue-50 p-5">
      <h2 class="text-base font-semibold text-blue-900 mb-2">How template keys work</h2>
      <ol class="list-decimal list-inside text-sm text-blue-900 space-y-1">
        <li>Each system email references a <code class="font-mono bg-blue-100 px-1 rounded">{{key}}</code>. Defaults come from code or by running <code class="font-mono bg-blue-100 px-1 rounded">rails email_templates:sync_defaults</code>.</li>
        <li>Super admins can edit subjects/bodies here without a deployment. Locale overrides are optional.</li>
        <li>Use placeholders like <code class="font-mono bg-blue-100 px-1 rounded">{{organization_name}}</code>. A help link inside each template shows the available data.</li>
      </ol>
    </div>

    <%= render "shared/filters" %>

    <div class="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden">
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Name</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Key</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Default Locale</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
            <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
          <% @email_template_keys.each do |key| %>
            <tr class="hover:bg-gray-50">
              <td class="px-6 py-4 whitespace-nowrap">
                <div class="text-sm font-semibold text-gray-900"><%= key.name %></div>
                <div class="text-sm text-gray-500 truncate max-w-xs"><%= key.description %></div>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm font-mono text-gray-900">
                <%= key.key %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                <%= key.default_locale %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap">
                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium <%= key.active? ? 'bg-green-100 text-green-800' : 'bg-gray-100 text-gray-800' %>">
                  <%= key.active? ? "Active" : "Inactive" %>
                </span>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                <%= link_to "View", admin_email_template_key_path(key), class: "text-blue-600 hover:text-blue-800 mr-3" %>
                <%= link_to "Edit", edit_admin_email_template_key_path(key), class: "text-indigo-600 hover:text-indigo-800" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <% if @email_template_keys.empty? %>
        <div class="text-center py-12">
          <p class="text-gray-500 text-lg">No template keys found</p>
          <p class="text-gray-400 text-sm mt-2">Create a template key to get started.</p>
        </div>
      <% end %>
    </div>
  </div>
</div>

