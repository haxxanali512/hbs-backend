<div class="min-h-screen bg-gray-50 py-8">
  <div class="w-full mx-auto px-4 sm:px-6 lg:px-8 max-w-4xl">
    <div class="mb-6 flex items-center justify-between">
      <div>
        <h1 class="text-2xl font-bold text-gray-900">Edit Email Template</h1>
        <p class="mt-2 text-gray-600"><%= @email_template.email_template_key.name %> · <%= @email_template.locale %></p>
      </div>
      <%= link_to "Back", admin_email_template_path(@email_template), class: "text-blue-600 hover:text-blue-800" %>
    </div>

    <div class="bg-white border border-gray-200 rounded-lg shadow-sm p-6">
      <%= render "form",
                form_model: @email_template,
                form_url: admin_email_template_path(@email_template),
                cancel_path: admin_email_template_path(@email_template) %>
    </div>
  </div>
</div>

