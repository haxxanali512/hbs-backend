<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        line-height: 1.6;
        color: #374151;
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        padding: 30px;
        text-align: center;
        border-radius: 8px 8px 0 0;
      }
      .content {
        background: white;
        padding: 30px;
        border: 1px solid #e5e7eb;
        border-top: none;
      }
      .footer {
        background: #f9fafb;
        padding: 20px;
        text-align: center;
        border-radius: 0 0 8px 8px;
        border: 1px solid #e5e7eb;
        border-top: none;
        font-size: 14px;
        color: #6b7280;
      }
      .button {
        display: inline-block;
        background: #4f46e5;
        color: white;
        padding: 12px 24px;
        text-decoration: none;
        border-radius: 6px;
        font-weight: 500;
        margin: 20px 0;
      }
      .info-box {
        background: #f3f4f6;
        border-left: 4px solid #4f46e5;
        padding: 20px;
        margin: 20px 0;
      }
      .alert-box {
        background: #fef3c7;
        border-left: 4px solid #f59e0b;
        padding: 20px;
        margin: 20px 0;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1>Manual Payment Request</h1>
      <p>New billing setup requires your approval</p>
    </div>
    
    <div class="content">
      <h2>Organization Details</h2>
      <div class="info-box">
        <p><strong>Organization:</strong> <%= @organization.name %></p>
        <p><strong>Owner:</strong> <%= @owner.email %></p>
        <p><strong>Tier:</strong> <%= @organization.tier || 'N/A' %></p>
        <p><strong>Requested At:</strong> <%= @organization_billing.created_at.strftime("%B %d, %Y at %I:%M %p") %></p>
      </div>

      <div class="alert-box">
        <h3>Action Required</h3>
        <p>This organization has requested manual payment processing for their billing setup. Please review their information and approve or reject their request.</p>
      </div>

      <p>To review and manage this request, please visit the admin panel:</p>
      
      <a href="<%= admin_organization_billing_url(@organization_billing) %>" class="button">Review Request</a>
      
      <p>You can also view all pending requests in the <a href="<%= admin_organization_billings_url %>">Billing Management</a> section.</p>
    </div>
    
    <div class="footer">
      <p>This is an automated message from HBS Data Processing System.</p>
      <p>Please do not reply to this email. Contact the organization owner directly if needed.</p>
    </div>
  </body>
</html>
