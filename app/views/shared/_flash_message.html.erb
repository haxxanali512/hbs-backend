<% type ||= flash.keys.first&.to_sym || :notice %>
<% message ||= flash[type] %>

<% if message.present? %>
  <div class="mb-4 rounded-md p-4 <%= case type.to_s
    when :notice, 'notice' then 'bg-green-50 border border-green-200 text-green-800'
    when :alert, 'alert', :error, 'error' then 'bg-red-50 border border-red-200 text-red-800'
    else 'bg-blue-50 border border-blue-200 text-blue-800'
  end %>" data-turbo-cache="false">
    <div class="flex">
      <div class="flex-shrink-0">
        <% if type.to_s == 'notice' || type == :notice %>
          <svg class="h-5 w-5 text-green-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.857-9.809a.75.75 0 00-1.214-.882l-3.483 4.79-1.88-1.88a.75.75 0 10-1.06 1.061l2.5 2.5a.75.75 0 001.137-.089l4-5.5z" clip-rule="evenodd" />
          </svg>
        <% else %>
          <svg class="h-5 w-5 text-red-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.28 7.22a.75.75 0 00-1.06 1.06L8.94 10l-1.72 1.72a.75.75 0 101.06 1.06L10 11.06l1.72 1.72a.75.75 0 101.06-1.06L11.06 10l1.72-1.72a.75.75 0 00-1.06-1.06L10 8.94 8.28 7.22z" clip-rule="evenodd" />
          </svg>
        <% end %>
      </div>
      <div class="ml-3 flex-1">
        <p class="text-sm font-medium"><%= message %></p>
      </div>
      <div class="ml-auto pl-3">
        <button type="button" class="inline-flex rounded-md p-1.5 <%= case type.to_s
          when :notice, 'notice' then 'text-green-500 hover:bg-green-100'
          when :alert, 'alert', :error, 'error' then 'text-red-500 hover:bg-red-100'
          else 'text-blue-500 hover:bg-blue-100'
        end %>" onclick="this.closest('div').remove()">
          <svg class="h-4 w-4" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
            <path d="M6.28 5.22a.75.75 0 00-1.06 1.06L8.94 10l-3.72 3.72a.75.75 0 101.06 1.06L10 11.06l3.72 3.72a.75.75 0 101.06-1.06L11.06 10l3.72-3.72a.75.75 0 00-1.06-1.06L10 8.94 6.28 5.22z" />
          </svg>
        </button>
      </div>
    </div>
  </div>
<% end %>

