<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=utf-8" http-equiv="Content-Type" />
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        background-color: #dc3545;
        color: white;
        padding: 15px;
        border-radius: 5px 5px 0 0;
      }
      .content {
        background-color: #f8f9fa;
        padding: 20px;
        border: 1px solid #dee2e6;
      }
      .failed-list {
        background-color: white;
        padding: 15px;
        margin: 15px 0;
        border-left: 4px solid #dc3545;
      }
      .successful-list {
        background-color: white;
        padding: 15px;
        margin: 15px 0;
        border-left: 4px solid #28a745;
      }
      table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 10px;
      }
      th, td {
        padding: 8px;
        text-align: left;
        border-bottom: 1px solid #dee2e6;
      }
      th {
        background-color: #f8f9fa;
        font-weight: bold;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h2>Waystar EDI Submission Failures</h2>
      </div>
      <div class="content">
        <p>Dear <%= @organization.name %> Team,</p>
        
        <p>We encountered issues while submitting encounters to Waystar via EDI 837:</p>
        
        <div class="failed-list">
          <h3>Failed Submissions (<%= @failed_count %>)</h3>
          <% if @results[:failed].any? %>
            <table>
              <thead>
                <tr>
                  <th>Encounter ID</th>
                  <th>Patient Name</th>
                  <th>Date of Service</th>
                  <th>Error</th>
                </tr>
              </thead>
              <tbody>
                <% @results[:failed].each do |failure| %>
                  <tr>
                    <td><%= failure[:encounter_id] %></td>
                    <td><%= failure[:patient_name] %></td>
                    <td><%= failure[:date_of_service] %></td>
                    <td><%= failure[:error] %></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          <% else %>
            <p>No failures to report.</p>
          <% end %>
        </div>
        
        <% if @results[:successful].any? %>
          <div class="successful-list">
            <h3>Successful Submissions (<%= @successful_count %>)</h3>
            <p>The following encounters were successfully submitted:</p>
            <ul>
              <% @results[:successful].each do |success| %>
                <li>
                  Encounter #<%= success[:encounter_id] %> - 
                  <%= success[:patient_name] %> 
                  (<%= success[:date_of_service] %>)
                </li>
              <% end %>
            </ul>
          </div>
        <% end %>
        
        <p>Please review the failed submissions and take appropriate action.</p>
        
        <p>Best regards,<br>HBS Data Processing System</p>
      </div>
    </div>
  </body>
</html>

